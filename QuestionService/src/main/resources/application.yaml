spring:
  application:
    name: QuestionService

  datasource:
    url: jdbc:mysql://localhost:3306/quizzy
    username: ${db-username}
    password: ${db-password}

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082